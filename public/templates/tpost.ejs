<%  if (!preview) { -%>
; tpost<%- toolNumber %>.g
; called after tool <%- toolNumber %> has been selected
;
; generated by RepRapFirmware Configuration Tool v<%- version %> on <%- (new Date()).toString() %>

<%	}
    const _custom = model.configTool.toolPostCommands.get(String(toolNumber));
    const _wait = model.configTool.waitForToolTemperatures;
    if (_wait) { -%>
M116 P<%- toolNumber %> ; wait for tool temperatures to be reached
<%	}
    if (_custom && _custom.trim() !== "") { -%>
<%- _custom %>
<%	} else if (!_wait && preview) { -%>
; No content in this file
<%  } -%>
