<%  if (!preview) { -%>
; tpre<%- toolNumber %>.g
; called before tool <%- toolNumber %> is selected
;
; generated by RepRapFirmware Configuration Tool v<%- version %> on <%- (new Date()).toString() %>

<%	}
    const _custom = model.configTool.toolPreCommands.get(String(toolNumber));
    if (_custom && _custom.trim() !== "") { -%>
<%- _custom %>
<%  } else if (preview) { -%>
; No content in this file
<%  } -%>
