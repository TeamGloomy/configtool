<%  if (!preview) { -%>
; tfree<%- toolNumber %>.g
; called when tool <%- toolNumber %> is freed
;
; generated by RepRapFirmware Configuration Tool v<%- version %> on <%- (new Date()).toString() %>

<%  }
    const _custom = model.configTool.toolFreeCommands.get(String(toolNumber));
    if (_custom && _custom.trim() !== "") { -%>
<%- _custom %>
<%  } else if (preview) { -%>
; No content in this file
<%  } -%>
